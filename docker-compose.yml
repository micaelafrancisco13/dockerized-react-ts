version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5173:5173
    volumes:
      - .:/app
    command: ./docker-entrypoint.sh
    # command: sh -c "npm rebuild esbuild && yarn dev"